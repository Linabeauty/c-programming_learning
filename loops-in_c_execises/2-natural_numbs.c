#include <stdio.h>

/**
 * main - print natural numbers using for loop style
 *
 * Return: Always successful
 */

int main(void)
{
	int i;

	for (i = 0; i <= 100; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
