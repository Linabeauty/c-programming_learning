#include <stdio.h>

/**
 * main - Logic operator , to determine which case is true or false
 * Return: 0.
 */

int main(void)
{
	int x = 50, y = 25;

	if (x < 50 &&  x < 25)
		printf("True!\n");
	else
		printf("Oh No Try Again!\n");
	return (0);
}
