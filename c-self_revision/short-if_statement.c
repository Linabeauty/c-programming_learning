#include <stdio.h>

/**
 * main - used to write a short line of code instead
 * of the long form of writing code
 * Return: 0.
 */

int main(void)
{
	int time = 15;

	/* used to confirm if it's noon or evening */
	(time > 15) ? printf("Good day.\n") : printf("Good evening.\n");
	return (0);
}
