#include <stdio.h>

/**
 * main - find the maximum among two numbers
 *
 * Return: Always 0
 */

int main(void)
{
	int x = 200;
	int y = 350;

	if (x > y)
	{
		printf("The maximum number is %d\n", x);
	}

	else
	{
		printf("The maximum number is %d\n", y);
	}
	return (0);
}
